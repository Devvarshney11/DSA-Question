#include<iostream>
#include<algorithm>
#include<string>
#include<vector>

using namespace std;


//By Array Method
string longestCommonPrefix(vector<string> &str,int n)
{
    for(int i = 0;i<str[0].size();i++)
    {
        char ch = str[i
        
        ]
    }
}
int main()
{
return 0;
}